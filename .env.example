# Copy to .env.local for local scripting use (not read by the browser -
# the browser reads assets/js/supabase-config.js instead, see docs/DEPLOYMENT.md).
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=replace-with-local-anon-key-from-supabase-status
